// import * as actionTypes from './constants';

const defaultState = {
    recommendList:[]
}

export default (state, action) => {
    switch(action.type) {
        default:
            return state
    }
}